{% extends 'base.html.twig' %}

{% block title %}Dashboard Admin | EcoRide{% endblock %}

{% block body %}
<main class="pt-5 mt-5 flex-grow-1">
    <div class="container">

        <h1 class="text-eco-green fw-bold text-center mb-5">Dashboard Administrateur</h1>

        {% if lastSnapshot %}
            <div class="row text-center mb-4">
                <div class="col-md-4">
                    <div class="card card-eco">
                        <p class="mb-1"> Total trajets réalisés</p>
                        <h4>{{ lastSnapshot.nbTrajets }}</h4>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card card-eco">
                        <p class="mb-1"> Crédits gagnés</p>
                        <h4>{{ lastSnapshot.creditsGagnes }}</h4>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card card-eco">
                        <p class="mb-1">👥 Nombre d’utilisateurs</p>
                        <h4>{{ lastSnapshot.nbUtilisateurs }}</h4>
                    </div>
                </div>
            </div>
        {% else %}
            <p class="text-center text-muted">Aucune statistique disponible (exécute la commande <code>app:collect-stats</code>).</p>
        {% endif %}

        <div class="row mt-5">
            <div class="col-md-6 mb-4">
                <h5 class="text-eco-green text-center">Trajets réalisés par jour</h5>
                <canvas id="chartTrajets"></canvas>
            </div>
            <div class="col-md-6 mb-4">
                <h5 class="text-eco-green text-center">Crédits gagnés par jour</h5>
                <canvas id="chartCredits"></canvas>
            </div>
        </div>

    </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
fetch("{{ path('admin_stats_data') }}")
    .then(res => res.json())
    .then(data => {
        new Chart(document.getElementById('chartTrajets'), {
            type: 'line',
            data: {
                labels: data.trajets.labels,
                datasets: [{
                    label: 'Trajets réalisés',
                    data: data.trajets.values,
                    borderWidth: 2
                }]
            }
        });

        new Chart(document.getElementById('chartCredits'), {
            type: 'line',
            data: {
                labels: data.credits.labels,
                datasets: [{
                    label: 'Crédits gagnés',
                    data: data.credits.values,
                    borderWidth: 2
                }]
            }
        });
    });
</script>
{% endblock %}

